class Solution {
  public:
    int findMaxValue(vector<vector<int>>& mat, int N) {
        
        // first we will try to find a minimum value
        // and everytime we also find a maximum value corresponding
        // to that minimum value
        // keeping the condition c > a and d > b in mind
        
        int mini = INT_MAX;
        
        for(int i=0 ; i< ; i++) {
            for(int j=0 ; j< ; j++) {
                
            }
        }
        
    }
};
